<UserControl x:Class="BrowserExtension.Views.CompleteSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrowserExtension.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Margin="5" Name="btn_Back">zurück</Button>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <TextBlock Name="tb_URL" Margin="5">URL</TextBlock>
            <local:SearchResults x:Name="sr_List" Margin="5" Width="905" Height="735"/>
        </StackPanel>
    </Grid>
</UserControl>
