<UserControl x:Class="BrowserExtension.Views.BrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrowserExtension.Views"
             mc:Ignorable="d" 
             d:DesignHeight="555" d:DesignWidth="1024">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <local:ButtonList Margin="5" Grid.Column="0" />
        <StackPanel Grid.Column="1">
            <TextBlock Name="tbLink" Margin="5">Link</TextBlock>
            <WebBrowser Name="webBrowser" HorizontalAlignment="Left" Height="750" Margin="5" VerticalAlignment="Top" Width="900"/>
        </StackPanel>
    </Grid>
</UserControl>
