<UserControl x:Class="BrowserExtension.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrowserExtension.Views"
             xmlns:viewModels="clr-namespace:BrowserExtension.ViewModels"
             mc:Ignorable="d" Height="800" Width="1024"
             >
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:MainViewModel}">
            <local:CompleteSearchView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ContentControl Content="{Binding CurrentContentControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
